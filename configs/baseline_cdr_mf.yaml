seed: 2026
device: auto
run_dir: runs/baseline_cdr_mf
baseline:
  name: cdr_mf
data:
  root: data/sample
  max_seq_len: 20
  neg_per_pos: 2
  num_workers: 0
model:
  embedding_dim: 64
train:
  batch_size: 128
  epochs: 5
  lr: 0.001
  weight_decay: 0.000001
  topk: [10, 20]

